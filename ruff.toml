target-version = "py312"
line-length = 100

[lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[lint.per-file-ignores]
"customizer/server.py" = ["E402"]  # sys.path must be set before framework imports
